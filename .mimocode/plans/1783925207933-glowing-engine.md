# План: Финальная доработка layout профиля

## Что нужно сделать

### 1. Убрать пустоту между hero баннером и левым сайдбаром
**Файл:** `src/app/profile/[userId]/page.tsx`

Левый сайдбар (Действия, Контакты, Статистика) остаётся. Нужно убрать лишний gap/отступ между hero баннером и началом сайдбара. Сайдбар должен начинаться сразу под hero баннером.

### 2. Hero баннер — расширить и сделать выше
**Файл:** `src/components/ProfileHeroBanner.tsx`

- Убрать `max-w-xl` — баннер растягивается на всю ширину центрального контента (как блок аналитики)
- Увеличить высоту: `h-72 md:h-[420px]` — книжный формат
- Округлённые углы сохранить

### 3. Всё под hero — одинаковая ширина
Вкладки и контент под ними = ширина hero баннера = ширина блока аналитики.

### 4. Боковые баннеры — fixed на краях
Левые: `left-0`, правые: `right-0`, `w-72`, `fixed top-20`, `z-0`. Уже сделано — проверить что работает.

### 5. Ключевые файлы

| Файл | Что менять |
|------|-----------|
| `src/app/profile/[userId]/page.tsx` | Убрать gap между hero и сайдбаром |
| `src/components/ProfileHeroBanner.tsx` | Убрать max-w-xl, увеличить высоту |

### 6. Верификация
1. `npx tsc --noEmit`
2. Деплой на сервер
3. Проверить: hero широкий и высокий, сайдбар сразу под ним без пустоты, боковые баннеры fixed
