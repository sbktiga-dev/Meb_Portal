# Статус исправлений — 27.06.2026

## Итого за сессию (27.06.2026)

### Исправленные баги

| # | Проблема | Решение | Файлы |
|---|----------|---------|-------|
| 1 | Лайки — рассинхронизация данных | UI использует `post.likes` из API, а не `_count.likesList` | `feed/page.tsx` |
| 2 | Лайки — сердечки не подсвечены при загрузке | Feed API возвращает `likedPostIds`, фронт предзаполняет `likedPosts` | `api/feed/route.ts`, `feed/page.tsx` |
| 3 | Уведомления не отображаются в dashboard | Добавлен JSX-блок рендеринга уведомлений | `dashboard/page.tsx` |
| 4 | Dropdown "Участники" не открывался на ПК | Вынесен из `nav` с `overflow-x-auto` в отдельный `div` | `Header.tsx` |
| 5 | "Добро пожаловать" не видно на тёмном фоне | Белый текст + `drop-shadow-sm` | `dashboard/page.tsx` |
| 6 | Специалисты не отображаются на `/specialists` | При регистрации `USER` автоматически создаётся запись `Specialist` | `api/auth/register/route.ts` |
| 7 | Build падал из-за пререндеринга главной | Главная страница сделана `force-dynamic` | `page.tsx` |

### Новые фичи

| # | Фича | Описание | Файлы |
|---|------|----------|-------|
| 1 | Статистика лайков в dashboard | Карточка "Лайков" с суммой по постам | `dashboard/page.tsx` |
| 2 | Роль "Клиент" при регистрации | "Клиент — хочу заказать мебель" (первый вариант в dropdown) | `register/page.tsx`, `api/auth/register/route.ts` |
| 3 | Загрузка обложки событий | Input для загрузки `coverImage` через Vercel Blob | `events/page.tsx` |
| 4 | События — сортировка | Активные события сверху, завершённые внизу | `api/events/route.ts` |
| 5 | Автоудаление событий | Завершённые события удаляются через 48ч после `endDate` | `api/events/route.ts` |
| 6 | SVG-фавиконка | Белый домик на оранжевом фоне | `public/favicon.svg`, `layout.tsx` |
| 7 | Eye toggle на пароле | Глазик в login + register, показывает/скрывает пароль | `login/page.tsx`, `register/page.tsx` |
| 8 | Lightbox для картинок | Full-screen просмотр с zoom (ПК: double-click, Мобилка: pinch-to-zoom) | `Lightbox.tsx`, `feed/page.tsx`, `dashboard/portfolio/page.tsx` |

### Изменения в навбаре

- Ссылки "Поставщики/Производства/Компании/Специалисты" объединены в dropdown "Участники"
- На мобилке — отдельная секция "Участники" в бургер-меню

---

## Build статус
- ✅ `npx next build` — успешно
- ✅ Vercel деплой — Ready

---

## Осталось (не критично)

- Добавить lightbox в галерею, товары, детальную страницу поста
- Расширить подписи разделов в админке
- Проверить Blob URL в админке-изображениях
